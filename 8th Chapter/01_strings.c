#include <stdio.h>

int main()
{
    // char str[]={'d','h','i','r','a','j'};
    char str[] = "dhiraj";
    for (int i = 0; i < 6; i++)
    {
        printf("%c", str[i]);
    }

    return 0;
}
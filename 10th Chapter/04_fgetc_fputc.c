#include <stdio.h>

int main()
{
    FILE *ptr;
    ptr = fopen("file2.txt", "a");
    // char a = fgetc(ptr); // used to read a cherecter form a file.
    //  printf("%c", a);
    //   fputc('c', ptr);
    return 0;
}
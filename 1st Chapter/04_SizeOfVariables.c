#include <stdio.h>
int main()
{
    printf("size of int is %d  bytes \n", sizeof(int));
    printf("size of char is %d bytes \n", sizeof(char));
    printf("size of float is %d bytes \n", sizeof(float));
    return 0;
}